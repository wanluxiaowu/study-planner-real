services:
  - type: web
    name: study-planner-real
    env: node
    buildCommand: ""
    startCommand: "node server.mjs"
    healthCheckPath: /api/health
    plan: starter
    disk:
      name: study-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
